# Backend Configuration
PORT=5001

# Optional: Provide an explicit path to your Python executable.
# If not set, the server auto-detects .venv/Scripts/python.exe (Windows)
# or .venv/bin/python (Linux/macOS), then falls back to the system python.
# PYTHON_PATH=C:\Path\To\python.exe
